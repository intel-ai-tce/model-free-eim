# vLLM Recipes tools staging directory

Run `./scripts/prepare-recipes.sh` before `docker build`. The script replaces
this placeholder with the selected vLLM `tools/recipes` tooling. This directory
does not contain the online model or hardware recipe data.
